{
  "name": "cozy-elea-poc",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build:injections": "node scripts/buildInjectedScripts.js",
    "start": "npm run build:injections && expo start",
    "android": "npm run build:injections && expo start --android",
    "ios": "npm run build:injections && expo start --ios",
    "web": "npm run build:injections && expo start --web"
  },
  "dependencies": {
    "@react-native-async-storage/async-storage": "^2.2.0",
    "expo": "~54.0.33",
    "expo-status-bar": "~3.0.9",
    "react": "19.1.0",
    "react-native": "0.81.5",
    "react-native-safe-area-context": "~5.6.0",
    "react-native-webview": "13.15.0"
  },
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.11",
    "eslint": "^9.0.0",
    "eslint-config-expo": "~10.0.0"
  }
}
